apiVersion: kustomize.config.k8s.io/v1beta1
configMapGenerator:
- behavior: merge
  files:
    - files/resource.customizations
  literals:
  - kustomize.buildOptions="--enable-alpha-plugins --enable-helm"
  - |
    repository.credentials=- passwordSecret:
        key: git_token
        name: autopilot-secret
      url: https://github.com/
      usernameSecret:
        key: git_username
        name: autopilot-secret
  # - |
  #   repositories=- type: helm
  #     name: helm-charts
  #     url: https://dafiti-group.github.io/helm-charts
  #     passwordSecret:
  #       key: password
  #       name: argocd-repositories-secrets
  #     usernameSecret:
  #       key: username
  #       name: argocd-repositories-secrets
  #   - type: git
  #     url: https://github.com/dafiti-group/helm-charts
  #     passwordSecret:
  #       key: password
  #       name: argocd-repositories-secrets
  #     usernameSecret:
  #       key: username
  #       name: argocd-repositories-secrets

  name: argocd-cm
kind: Kustomization
namespace: argocd
resources:
- github.com/argoproj-labs/argocd-autopilot/manifests/base?ref=v0.4.4
