apiVersion: apps/v1
kind: Deployment
metadata:
  name: morpheus-api
spec:
  selector:
    matchLabels:
      app: morpheus-api
  template:
    metadata:
      labels:
        app: morpheus-api
    spec:
      containers:
        - name: morpheus-api
          image: morpheus-api:v1
          resources:
            limits:
              memory: "128Mi"
              cpu: "500m"
          ports:
            - containerPort: 80
            - containerPort: 3000
---
kubernetes apiVersion: v1
kind: Service
metadata:
  name: morpheus-api
  labels:
    app: morpheus-api
    version: v1
spec:
  selector:
    app: morpheus-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
    - name: socket
      protocol: TCP
      port: 3000
      targetPort: 3000
    
